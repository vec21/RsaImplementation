#ifndef PRIME_FUNCTIONS_H
#define PRIME_FUNCTIONS_H

#include <vector>
#include <random>

// Função para verificar se um número é primo
bool era(long long n);

// Função para gerar um número primo no intervalo [min, max]
long long generatePrime(long long min, long long max);

//void radom();

#endif  // PRIME_FUNCTIONS_H
